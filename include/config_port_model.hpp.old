#ifndef CONFIG_PORT_MODEL_HPP
#define CONFIG_PORT_MODEL_HPP

#include <gtkmm.h>

class PortSpeedComboBoxModel : public Gtk::TreeModel::ColumnRecord
{
    public:
        PortSpeedComboBoxModel()
        { add(speedName);}

        Gtk::TreeModelColumn<Glib::ustring> speedName;
};

#endif
